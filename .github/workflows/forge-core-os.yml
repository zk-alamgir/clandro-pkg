name: Forge Core OS (aarch64)

on:
  workflow_dispatch:

jobs:
  build-core:
    runs-on: ubuntu-22.04 # Use a stable Ubuntu version
    steps:
      - name: Checkout Sovereign Code
        uses: actions/checkout@v4

      - name: Free Disk Space (Maximize Build RAM)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true

      - name: Setup Master Environment & NDK
        run: |
          # This official script downloads the exact Android NDK and sets up the environment
          sudo ./scripts/setup-ubuntu.sh

      - name: Forge Tier 1 Packages (The Bootstrap)
        run: |
          # Now the NDK is installed, we can compile the core OS directly
          ./build-package.sh -I -a aarch64 bash
          ./build-package.sh -I -a aarch64 coreutils
          ./build-package.sh -I -a aarch64 dpkg
          ./build-package.sh -I -a aarch64 apt
          ./build-package.sh -I -a aarch64 ncurses
          ./build-package.sh -I -a aarch64 readline
          ./build-package.sh -I -a aarch64 termux-tools
          ./build-package.sh -I -a aarch64 ca-certificates

      - name: Generate APT Index (The Repository Map)
        run: |
          sudo apt-get update && sudo apt-get install -y dpkg-dev
          cd output
          dpkg-scanpackages -m . > Packages
          gzip -9c Packages > Packages.gz

      - name: Upload Core OS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cl-andro-core-debs-aarch64
          path: output/